% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_stations.R
\name{get_stations}
\alias{get_stations}
\title{Get BirdWeather Stations}
\usage{
get_stations(
  query = NULL,
  from = NULL,
  to = NULL,
  ne = NULL,
  sw = NULL,
  limit = NULL
)
}
\arguments{
\item{query}{Optional search string to filter stations by name (optional)}

\item{from}{Start datetime in ISO8601 format (optional)}

\item{to}{End datetime in ISO8601 format (optional)}

\item{ne}{Named list with lat and lon defining the north-east corner of a
bounding box (optional). Must be used together with sw.}

\item{sw}{Named list with lat and lon defining the south-west corner of a
bounding box (optional). Must be used together with ne.}

\item{limit}{Maximum total number of stations to return (default: NULL,
returns all matching stations). Each page fetches 250 at a time.}
}
\value{
A flat data.table where each row is one station with columns:
station_id, station_name, station_type, station_timezone,
station_country, station_continent, station_state, station_location,
station_lat, station_lon, location_privacy
}
\description{
Retrieves public BirdWeather stations with optional filters.
Handles pagination automatically and returns a flat data.table.
}
\note{
The fields \code{latestDetectionAt} and \code{earliestDetectionAt}
are not returned due to server-side performance limitations.
}
\examples{
\dontrun{
connect_birdweather()

# Get all stations
stations <- get_stations()

# Search by name
stations <- get_stations(query = "backyard")

# Filter by bounding box
stations <- get_stations(
  ne = list(lat = 42.0, lon = -85.0),
  sw = list(lat = 36.0, lon = -96.0)
)
}
}
